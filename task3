import 'package:flutter/material.dart';

class Len extends StatefulWidget {
  const Len({super.key});

  @override
  State<Len> createState() => _LenState();
}

class _LenState extends State<Len> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Row(
        children: [
          Expanded(child: Container(color: Colors.amber)),
          Expanded(
            child: Container(color: const Color.fromARGB(255, 33, 110, 3)),
          ),
          Expanded(
            child: Container(color: const Color.fromARGB(255, 8, 149, 250)),
          ),
          Expanded(
            child: Container(color: const Color.fromARGB(255, 81, 7, 255)),
          ),
        ],
      ),
    );
  }
}
